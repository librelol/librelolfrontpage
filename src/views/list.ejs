<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>markdown</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            margin: 20px;
            padding: 20px;
            background-color: #f5f5f7;
            color: #333;
        }
        .markdown-list {
            max-width: 800px;
            margin: auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }
        .markdown-list ul {
            list-style-type: none;
            padding: 0;
        }
        .markdown-list li {
            margin: 10px 0;
        }
        .markdown-list a {
            text-decoration: none;
            color: #007aff;
            padding: 10px 15px;
            display: block;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .markdown-list a:hover {
            background-color: #e0e0e0;
        }
        .directory > a {
            font-weight: bold;
        }
        .directory ul {
            display: none;
            margin-left: 20px;
        }
        .directory.open > ul {
            display: block;
        }
    </style>
</head>
<body>
<div class="markdown-list">
    <a href="/" style="display: inline-block; margin-bottom: 20px; color: #007aff; text-decoration: none; font-size: 16px;">&larr; Back to Home</a>
    <h1>Documents</h1>
    <p>Click on a file to view its content:</p>
    <ul>
        <% function renderFiles(files) { %>
            <% files.forEach(file => { %>
                <% if (file.type === 'directory') { %>
                    <li class="directory">
                        <a href="#" class="toggle-directory"><%= file.name %></a>
                        <ul>
                            <%= renderFiles(file.children) %>
                        </ul>
                    </li>
                <% } else if (file.type === 'file') { %>
                    <li><a href="/<%= file.path.replace(/\\/g, '/').replace('.md', '') %>"><%= file.name %></a></li>
                <% } %>
            <% }) %>
        <% } %>
        <%= renderFiles(files) %>
    </ul>
</div>
<script nonce="<%= nonce %>">
    document.querySelectorAll('.toggle-directory').forEach(element => {
        element.addEventListener('click', function(event) {
            event.preventDefault();
            const parentLi = element.parentElement;
            parentLi.classList.toggle('open');
        });
    });
</script>
</body>
</html>