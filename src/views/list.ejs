<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LL Documents</title>
    <!-- Retro Font -->
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Press Start 2P', cursive; /* Retro monospace font */
            margin: 20px;
            padding: 20px;
            background-color: #2b2d42; /* Dark navy pastel */
            color: #ffffff; /* White text for contrast */
        }

        /* Retro container styling */
        .markdown-list {
            max-width: 800px;
            margin: auto;
            background-color: #3f4050; /* Dark muted gray-blue */
            padding: 20px;
            border-radius: 8px;
            border: 4px solid #ff6b6b; /* Bold red border */
            box-shadow: 5px 5px 0 #4a90e2; /* Retro 3D shadow */
            color: #dcdcdc; /* Light gray text inside */
        }

        h1 {
            text-align: center;
            font-size: 1.5em;
            color: #ffffff;
            text-shadow: 2px 2px 0 #ff6b6b; /* Retro text shadow effect */
        }

        p {
            font-size: 1em;
            color: #dcdcdc; /* Light gray text */
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            margin: 10px 0;
        }

        /* Retro-style link and list item styles */
        .markdown-list a {
            text-decoration: none;
            color: #4a90e2; /* Pastel blue link */
            padding: 10px 15px;
            display: block;
            border: 3px solid #ff6b6b; /* Bold red border around links */
            text-shadow: 1px 1px 0 #000; /* Retro text shadow */
            background-color: #2b2d42; /* Dark navy background */
            transition: background-color 0.3s ease;
        }

        .markdown-list a:hover {
            background-color: #ff6b6b; /* Hover effect with pastel red */
            color: #ffffff; /* White text on hover */
        }

        .directory > a {
            font-weight: bold;
            text-decoration: underline; /* Retro-style underlined links */
        }

        .directory ul {
            display: none;
            margin-left: 20px;
        }

        .directory.open > ul {
            display: block;
        }

        /* Add retro pixelated background */
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://i.imgur.com/oFc00Qh.png'); /* Retro pixel pattern */
            opacity: 0.2;
            pointer-events: none;
        }
    </style>
</head>
<body>
    <div class="markdown-list">
        <!-- Retro-style back link -->
        <a href="/" style="display: inline-block; margin-bottom: 20px; color: #4a90e2;">&larr; Back to Home</a>
        <h1>Documents</h1>
        <p>Click on a file to view its content:</p>
        <ul>
            <% function renderFiles(files) { %>
                <% files.forEach(file => { %>
                    <% if (file.type === 'directory') { %>
                        <li class="directory">
                            <a href="#" class="toggle-directory"><%= file.name %></a>
                            <ul>
                                <%= renderFiles(file.children) %>
                            </ul>
                        </li>
                    <% } else if (file.type === 'file') { %>
                        <li><a href="/<%= file.path.replace(/\\/g, '/').replace('.md', '') %>"><%= file.name %></a></li>
                    <% } %>
                <% }) %>
            <% } %>
            <%= renderFiles(files) %>
        </ul>
    </div>

    <script nonce="<%= nonce %>">
        // Toggle directories open and close
        document.querySelectorAll('.toggle-directory').forEach(element => {
            element.addEventListener('click', function(event) {
                event.preventDefault();
                const parentLi = element.parentElement;
                parentLi.classList.toggle('open');
            });
        });
    </script>
</body>
</html>
